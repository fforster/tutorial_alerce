<div id="objects_table" class="tw-bg-bg-card tw-border tw-border-border tw-rounded">
  {% if objects_list['items']|length > 0 %}
  <table class="tw-w-full tw-text-sm">
    <thead class="tw-text-left tw-text-text-secondary tw-text-xs tw-uppercase">
      <tr class="tw-border-b tw-border-border">
        <th class="tw-px-3 tw-py-2">Object ID</th>
        <th class="tw-px-3 tw-py-2">N detections</th>
        <th class="tw-px-3 tw-py-2">Class</th>
        <th class="tw-px-3 tw-py-2">Probability</th>
      </tr>
    </thead>
    <tbody>
    {% for obj in objects_list['items'] %}
      <tr class="tw-border-b tw-border-border/50 hover:tw-bg-bg-tertiary">
        <td class="tw-px-3 tw-py-2 mono">{{ obj['oid'] }}</td>
        <td class="tw-px-3 tw-py-2">{{ obj.get('n_det', '-') }}</td>
        <td class="tw-px-3 tw-py-2">{{ obj.get('class_name', '-') }}</td>
        <td class="tw-px-3 tw-py-2 mono">{{ obj.get('probability', '-') }}</td>
      </tr>
    {% endfor %}
    </tbody>
  </table>
  {% else %}
  <div class="tw-p-6 tw-text-sm tw-text-text-muted tw-text-center">
    {{ objects_list.get('info_message', 'Results will appear here.') }}
  </div>
  {% endif %}
</div>
